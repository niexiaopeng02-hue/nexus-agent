{
  "buildCommand": "cd frontend && npm ci && npm run build && cd .. && node scripts/prepare-vercel-static.mjs",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/api/index.py"
    }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  }
}
